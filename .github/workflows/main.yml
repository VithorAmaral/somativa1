name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build_job:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - run: python3-v

  test-job:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - run: python3 hello.py

  deploy-job:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - run: echo "Finalizado"
